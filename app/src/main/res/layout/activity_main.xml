<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent" android:layout_height="match_parent">

    <EditText
        android:id="@+id/top_text_input"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:width="300dp"/>

    <EditText
        android:id="@+id/top_text_input_bottom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/top_text_input"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:width="300dp"/>

    <Button
        android:id="@+id/login_screen_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/top_text_input_bottom"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="@string/place_holder_login"
         />

    <fragment
        android:id="@+id/fragment"
        android:name="com.cdk.onboarding.cdkapplication.CDKIconFragment"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_below="@id/login_screen_btn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="50dp"
        tools:layout="@layout/bottom_picture_fragment" />

</RelativeLayout>